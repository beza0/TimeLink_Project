# Yerel: cp .env.example .env  →  docker compose up -d --build api
# 8080 başka projede doluysa (npm run dev proxy): TimeLink API’yi örn. 8082’de yayınlayıp
# DEV_API_PROXY=http://localhost:8082  (vite.config proxy hedefi; istemciye sızmaz)
# DEV_API_PROXY=
#
# CANLI (https://www.tiempos.site): bu dosya OKUNMAZ.
# Render → tiempos-backend → Environment → aşağıdaki Brevo değişkenlerini ekleyin.

JWT_SECRET=local-dev-jwt-secret-at-least-32-chars-long

# =============================================================================
# Brevo SMTP — canlı (Render) + yerel test (aynı değerler)
# Panel: https://app.brevo.com → Transactional → SMTP & API → SMTP
#   Login     → SPRING_MAIL_USERNAME (paneldeki tam metin)
#   SMTP key  → SPRING_MAIL_PASSWORD (xsmtpsib-... ; API key DEĞİL)
# Senders     → doğrulanmış e-posta/domain → APP_MAIL_FROM ile aynı adres
# İletişim formu 503 + smtp_send_failed: çoğunlukla gönderici doğrulanmamış veya SMTP şifresi API key ile karıştırılmıştır; Render API log satırına bakın.
# =============================================================================
SPRING_MAIL_HOST=smtp-relay.brevo.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=
SPRING_MAIL_PASSWORD=
APP_MAIL_FROM=Tiempos <noreply@tiempos.site>
# false iken doğrulama e-postası atlanır; iletişim formu SMTP (SPRING_MAIL_*) hazırsa yine göndermeyi dener.
APP_MAIL_ENABLED=true
APP_MAIL_STRICT_ERRORS=false
SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=true
SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true
SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_REQUIRED=true

APP_PUBLIC_BASE_URL=https://www.tiempos.site

# İletişim formu (varsayılan tiempos.site@gmail.com)
APP_CONTACT_INBOX=tiempos.site@gmail.com

# Brevo Transactional API (HTTPS 443) — iletişim formu, kayıt doğrulama ve şifre sıfırlama (SMTP olmadan da çalışır).
# Panel: Transactional → SMTP & API → API keys → Create (Transactional e-posta izni). SMTP xsmtpsib- şifresi değildir.
# BREVO_API_KEY=

# Google Sign-In (GSI) — Web uygulaması OAuth 2.0 Client ID (.apps.googleusercontent.com)
# Console: Credentials → Web client → Authorized JavaScript origins:
#   http://localhost:5173  http://127.0.0.1:5173  https://www.tiempos.site  (kullandığınız origin’ler)
# "origin is not allowed for the given client ID" = origins listesinde bu URL yok.
# İkisi aynı Web client ID olmalı (backend id_token aud doğrulaması için GOOGLE_CLIENT_ID).
GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_ID=

# --- Yerel Mailpit (gerçek gelen kutusu yok, http://localhost:8025) ---
# SPRING_MAIL_HOST=mailpit
# SPRING_MAIL_PORT=1025
# SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=false
# SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=false
# SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_REQUIRED=false
# SPRING_MAIL_USERNAME=
# SPRING_MAIL_PASSWORD=
# APP_MAIL_FROM=Tiempos <noreply@tiempos.local>

# --- Gmail (sadece yerel deneme; Render’da kullanmayın) ---
# SPRING_MAIL_HOST=smtp.gmail.com
# SPRING_MAIL_PORT=587
# SPRING_MAIL_USERNAME=you@gmail.com
# SPRING_MAIL_PASSWORD=16-char-app-password-no-spaces
# APP_MAIL_FROM=Tiempos <you@gmail.com>
